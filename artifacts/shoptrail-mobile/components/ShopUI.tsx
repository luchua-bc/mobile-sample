import { Feather, Ionicons } from '@expo/vector-icons';
import React from 'react';
import { Image, Pressable, StyleSheet, Text, View } from 'react-native';
import type { Product } from '@/data/catalog';
import { useColors } from '@/hooks/useColors';
export function LogoMark({ compact = false }: { compact?: boolean }) { const colors = useColors(); return <View style={styles.logoRow}><View style={[styles.logoMark, { backgroundColor: colors.primary }]}><View style={[styles.logoDot, { backgroundColor: colors.background }]} /></View>{!compact && <Text style={[styles.logoText, { color: colors.foreground }]}>shoptrail</Text>}</View>; }
export function IconButton({ name, onPress, badge, size = 21, accessibilityLabel }: { name: keyof typeof Feather.glyphMap; onPress: () => void; badge?: number; size?: number; accessibilityLabel: string }) { const colors = useColors(); return <Pressable testID={accessibilityLabel} accessibilityLabel={accessibilityLabel} onPress={onPress} style={({ pressed }) => [styles.iconButton, { backgroundColor: colors.card }, pressed && styles.pressed]}><Feather name={name} size={size} color={colors.foreground} />{!!badge && <View style={[styles.badge, { backgroundColor: colors.primary }]}><Text style={styles.badgeText}>{badge}</Text></View>}</Pressable>; }
export function PrimaryButton({ label, onPress, disabled = false, icon }: { label: string; onPress: () => void; disabled?: boolean; icon?: keyof typeof Feather.glyphMap }) { const colors = useColors(); return <Pressable testID={label} accessibilityRole="button" accessibilityLabel={label} disabled={disabled} onPress={onPress} style={({ pressed }) => [styles.primaryButton, { backgroundColor: disabled ? colors.muted : colors.primary }, pressed && !disabled && styles.pressed]}><Text style={[styles.primaryButtonText, { color: disabled ? colors.mutedForeground : colors.primaryForeground }]}>{label}</Text>{icon && <Feather name={icon} size={17} color={disabled ? colors.mutedForeground : colors.primaryForeground} />}</Pressable>; }
export function SectionHeader({ title, action, onAction }: { title: string; action?: string; onAction?: () => void }) { const colors = useColors(); return <View style={styles.sectionHeader}><Text style={[styles.sectionTitle, { color: colors.foreground }]}>{title}</Text>{action && onAction && <Pressable onPress={onAction} hitSlop={8}><Text style={[styles.sectionAction, { color: colors.primary }]}>{action}</Text></Pressable>}</View>; }
export function ProductCard({ product, onPress, onAdd }: { product: Product; onPress: () => void; onAdd: () => void }) { const colors = useColors(); return <Pressable testID={product.id} onPress={onPress} style={({ pressed }) => [styles.productCard, { backgroundColor: colors.card }, pressed && styles.cardPressed]}><View style={[styles.productImageWrap, { backgroundColor: product.accent }]}><Image source={product.image} style={styles.productImage} resizeMode="cover" />{!!product.badge && <View style={[styles.productBadge, { backgroundColor: colors.background }]}><Text style={[styles.productBadgeText, { color: colors.foreground }]}>{product.badge}</Text></View>}<Pressable testID={product.name + '-add'} accessibilityLabel={'Add ' + product.name + ' to cart'} onPress={onAdd} style={({ pressed }) => [styles.addButton, { backgroundColor: colors.foreground }, pressed && styles.pressed]}><Feather name="plus" size={18} color={colors.background} /></Pressable></View><View style={styles.productInfo}><Text style={[styles.productCategory, { color: colors.mutedForeground }]}>{product.category.toUpperCase()}</Text><Text numberOfLines={1} style={[styles.productName, { color: colors.foreground }]}>{product.name}</Text><View style={styles.priceRow}><Text style={[styles.productPrice, { color: colors.foreground }]}>$ {product.price}</Text>{product.compareAt && <Text style={[styles.comparePrice, { color: colors.mutedForeground }]}>$ {product.compareAt}</Text>}</View></View></Pressable>; }
export function EmptyState({ icon, title, message }: { icon: keyof typeof Feather.glyphMap; title: string; message: string }) { const colors = useColors(); return <View style={styles.emptyState}><View style={[styles.emptyIcon, { backgroundColor: colors.muted }]}><Feather name={icon} size={25} color={colors.primary} /></View><Text style={[styles.emptyTitle, { color: colors.foreground }]}>{title}</Text><Text style={[styles.emptyMessage, { color: colors.mutedForeground }]}>{message}</Text></View>; }
export function Stars({ rating, reviews }: { rating: number; reviews: number }) { const colors = useColors(); return <View style={styles.stars}><Ionicons name="star" size={14} color={colors.accent} /><Text style={[styles.rating, { color: colors.foreground }]}>{rating}</Text><Text style={[styles.reviewCount, { color: colors.mutedForeground }]}>({reviews})</Text></View>; }
const styles = StyleSheet.create({ logoRow: { flexDirection: 'row', alignItems: 'center', gap: 9 }, logoMark: { width: 29, height: 29, borderRadius: 10, alignItems: 'center', justifyContent: 'center', transform: [{ rotate: '-10deg' }] }, logoDot: { width: 9, height: 9, borderRadius: 5 }, logoText: { fontFamily: 'Inter_700Bold', fontSize: 18, letterSpacing: -0.7 }, iconButton: { width: 42, height: 42, borderRadius: 21, alignItems: 'center', justifyContent: 'center' }, badge: { position: 'absolute', top: -2, right: -2, minWidth: 17, height: 17, borderRadius: 9, alignItems: 'center', justifyContent: 'center', paddingHorizontal: 3 }, badgeText: { color: '#FFFFFF', fontFamily: 'Inter_700Bold', fontSize: 10 }, primaryButton: { minHeight: 54, borderRadius: 17, alignItems: 'center', justifyContent: 'center', flexDirection: 'row', gap: 10, paddingHorizontal: 20 }, primaryButtonText: { fontFamily: 'Inter_600SemiBold', fontSize: 15 }, pressed: { opacity: 0.78, transform: [{ scale: 0.98 }] }, sectionHeader: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginBottom: 14 }, sectionTitle: { fontFamily: 'Inter_700Bold', fontSize: 21, letterSpacing: -0.6 }, sectionAction: { fontFamily: 'Inter_600SemiBold', fontSize: 13 }, productCard: { width: 166, borderRadius: 20, overflow: 'hidden' }, cardPressed: { opacity: 0.9, transform: [{ scale: 0.985 }] }, productImageWrap: { height: 187, position: 'relative', overflow: 'hidden' }, productImage: { width: '100%', height: '100%' }, productBadge: { position: 'absolute', top: 10, left: 10, paddingHorizontal: 9, paddingVertical: 6, borderRadius: 10 }, productBadgeText: { fontFamily: 'Inter_600SemiBold', fontSize: 10 }, addButton: { position: 'absolute', bottom: 10, right: 10, width: 34, height: 34, borderRadius: 17, alignItems: 'center', justifyContent: 'center' }, productInfo: { padding: 13 }, productCategory: { fontFamily: 'Inter_600SemiBold', fontSize: 9, letterSpacing: 1.1, marginBottom: 5 }, productName: { fontFamily: 'Inter_600SemiBold', fontSize: 14, marginBottom: 7 }, priceRow: { flexDirection: 'row', alignItems: 'center', gap: 7 }, productPrice: { fontFamily: 'Inter_700Bold', fontSize: 15 }, comparePrice: { fontFamily: 'Inter_400Regular', fontSize: 12, textDecorationLine: 'line-through' }, emptyState: { alignItems: 'center', paddingHorizontal: 32, paddingVertical: 54 }, emptyIcon: { width: 58, height: 58, borderRadius: 29, alignItems: 'center', justifyContent: 'center', marginBottom: 16 }, emptyTitle: { fontFamily: 'Inter_700Bold', fontSize: 20, marginBottom: 8, textAlign: 'center' }, emptyMessage: { fontFamily: 'Inter_400Regular', fontSize: 14, lineHeight: 21, textAlign: 'center' }, stars: { flexDirection: 'row', alignItems: 'center', gap: 4 }, rating: { fontFamily: 'Inter_600SemiBold', fontSize: 13 }, reviewCount: { fontFamily: 'Inter_400Regular', fontSize: 12 } });
